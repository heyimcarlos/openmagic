# OpenMagic playground

The playground foundation exposes only a synthetic safety manifest. It will
consume the installed API through public network interfaces and is verified
externally by `openmagic-evals`. It is never a production dependency.
