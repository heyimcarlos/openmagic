# Context policy follows evidence

The learner identified that a smaller prompt or one-Packet policy is not
inherently better. Retrieval correctness must remain a hard requirement, while
Packet counts and context burden should be measured across varied eval sets and
used to revise the policy when the data justifies it.
